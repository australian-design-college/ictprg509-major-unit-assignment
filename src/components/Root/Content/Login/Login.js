import React, { Component } from "react";
import { Well } from "react-bootstrap";

export default class Login extends Component {
  render() {
    return (
      <div>
        <h4>Login to access content</h4>
        <Well>This is where you should build the Login component.</Well>
      </div>
    );
  }
}
